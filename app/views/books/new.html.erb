<h2>New Book</h2>

<%= form_with model:@book do |f| %>
 <div class="field">
    <%= f.label :Name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
     <%= f.label :Opinion %><br />
     <%= f.text_area :opinion %>
  </div>
  <%= f.submit 'Create Book' %>
 <% end %>