<h2>New Book</h2>

<%= form_with model:book do |f| %>
 <div class="field">
    <%= f.label :Title %><br />
    <%= f.text_field :title %>
  </div>

  <div class="field">
     <%= f.label :Opinion %><br />
     <%= f.text_area :body %>
  </div>
  <%= f.submit 'Create Book' %>
 <% end %>