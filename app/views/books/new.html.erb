<% if @book.errors.any? %>　<!--エラーメッセージーー＞
  <%= @book.errors.count %>errors prohibited this obj from being saved:　
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<h2>New Book</h2>

<%= form_with model:@book do |f| %>
 <div class="field">
    <%= f.label :Name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
     <%= f.label :Opinion %><br />
     <%= f.text_area :opinion %>
  </div>
  <%= f.submit 'Create Book' %>
 <% end %>